apiVersion: v1
kind: Service
metadata:
  name: cloud-jpa-service
  namespace: study
  labels:
    app: cloud-jpa
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: cloud-jpa
